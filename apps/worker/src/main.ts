// Worker entrypoint — BullMQ processors register here (parser, verification, billing, notifications).
// Runs as a separate ECS service from the API. Placeholder until the queues workstream starts.
console.log('nabhahita-worker: no queues registered yet');
