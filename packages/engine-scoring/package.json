{
  "name": "@nabhahita/engine-scoring",
  "version": "0.0.0",
  "private": true,
  "description": "Pure scoring engine: (profile, preset) → score + explanation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(lint) @nabhahita/engine-scoring — no linter configured yet\"",
    "test": "echo \"(test) @nabhahita/engine-scoring — no tests yet\""
  },
  "devDependencies": {
    "@nabhahita/config": "workspace:*",
    "typescript": "~5.9.3"
  }
}
