{
  "name": "@ursainyk/engine-scoring",
  "version": "0.0.0",
  "private": true,
  "description": "Pure scoring engine: (profile, preset) → score + explanation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(lint) @ursainyk/engine-scoring — no linter configured yet\"",
    "test": "echo \"(test) @ursainyk/engine-scoring — no tests yet\""
  },
  "devDependencies": {
    "@ursainyk/config": "workspace:*",
    "typescript": "~5.9.3"
  }
}
