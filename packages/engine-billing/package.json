{
  "name": "@ursainyk/engine-billing",
  "version": "0.0.0",
  "private": true,
  "description": "Pure billing/payout engine: verification results \u2192 invoice & payout lines (integer paise)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(lint) @ursainyk/engine-billing \u2014 no linter configured yet\"",
    "test": "tsx --test test/*.test.ts"
  },
  "devDependencies": {
    "@ursainyk/config": "workspace:*",
    "typescript": "~7.0.2",
    "tsx": "^4.20.0",
    "@types/node": "^24.0.0"
  }
}