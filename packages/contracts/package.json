{
  "name": "@nabhahita/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "zod schemas shared by api and worker; source of the OpenAPI contract",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(lint) @nabhahita/contracts — no linter configured yet\"",
    "test": "echo \"(test) @nabhahita/contracts — no tests yet\""
  },
  "devDependencies": {
    "@nabhahita/config": "workspace:*",
    "typescript": "~5.9.3"
  }
}
