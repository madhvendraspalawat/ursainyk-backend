{
  "name": "@ursainyk/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "zod schemas shared by api and worker; source of the OpenAPI contract",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(lint) @ursainyk/contracts — no linter configured yet\"",
    "test": "echo \"(test) @ursainyk/contracts — no tests yet\""
  },
  "devDependencies": {
    "@ursainyk/config": "workspace:*",
    "typescript": "~7.0.2"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
